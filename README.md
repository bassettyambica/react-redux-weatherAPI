# Weather API using React and Redux
Helper Libraries : react-spaklines, GoogleMaps, OpenWheatherapi.
> cd ReduxSimpleStarter
> npm install
> npm start

>navigate to : "http://localhost:8080/" in browser.
